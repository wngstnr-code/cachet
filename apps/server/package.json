{
  "name": "@cachet/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cachet gateway (Person A): REST + EIP-712 signer + ChainClient. x402 & MCP menyusul (PR-4).",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cachet/contracts-abi": "file:../../packages/contracts-abi",
    "@fastify/rate-limit": "^10.2.0",
    "dotenv": "^16.4.5",
    "fastify": "^5.1.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
