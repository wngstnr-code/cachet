{
  "name": "cachet-cert-page",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cachet certificate proof page — reads X Layer directly, no backend",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fontsource-variable/jetbrains-mono": "^5.2.5",
    "@fontsource-variable/source-serif-4": "^5.2.5",
    "viem": "^2.31.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.8.0",
    "vite": "^6.3.0"
  }
}
