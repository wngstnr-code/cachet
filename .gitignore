 # dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
*.tsbuildinfo

# python
__pycache__/
*.pyc
.pytest_cache/
venv/
.venv/

# move
contracts/**/build/

# foundry (Person B)
contracts/out/
contracts/cache/
contracts/broadcast/**/dry-run/
# broadcast/ SELAIN dry-run sengaja di-track: berisi alamat & tx hash hasil
# deploy testnet — bukti on-chain yang dipakai saat serah-terima H3.

# env — NEVER commit real secrets (private keys, API keys, Supabase creds)
# `.env.*` (bukan cuma `.env.local`) karena deploy per-chain memakai file
# terpisah seperti .env.mainnet — pola lama tidak menangkapnya, dan file berisi
# DEPLOYER_PK mainnet yang lolos ke git tidak bisa ditarik kembali.
.env
.env.*
*.local
env-*.txt

# Template WAJIB tetap ter-track — pengecualian ini harus di bawah `.env.*`,
# kalau tidak polanya menelan template juga dan onboarding jadi rusak.
!.env.example
!.env.mainnet.example
.env.mainnet

# logs
*.log
npm-debug.log*

# editor / os
.DS_Store
.idea/
.vscode/

# local agent state

# CLAUDE.md
task.txt

graphify-out/

# Dokumen perencanaan internal — tidak untuk publik/juri (tetap ada di disk lokal)
docs/
demo-video.md
cachet_idea_v2.md
.vercel
